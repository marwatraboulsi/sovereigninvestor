/**
 * Sovereign Investor - Design Token System
 *
 * Palette sourced exactly from marwatraboulsi.substack.com:
 *   Background:  #0d1f1b  (deep forest teal)
 *   Accent/Gold: #c1a35e  (warm gold)
 *   Text:        #e5eae9  (warm off-white)
 *   Fonts:       Lora (serif headings), Spectral (serif body)
 */

// ─── Colours ──────────────────────────────────────────────────────────────────

export const BG   = '#0d1f1b';  // web background - deep forest teal
export const S1   = '#1c2c29';  // surface 1 - cards, lists   (contrast-1)
export const S2   = '#2e3d3a';  // surface 2 - inputs, elevated (contrast-2)
export const LINE = '#2e3d3a';  // hairline divider
export const W    = '#e5eae9';  // primary text - warm off-white
export const GOLD = '#c1a35e';  // pop accent - warm gold
export const G1   = '#9ea5a4';  // secondary text              (contrast-4/5)
export const G2   = '#747e7c';  // tertiary text               (contrast-4)
export const G3   = '#515e5b';  // ghost / very subtle         (contrast-3)

// ─── Typography ───────────────────────────────────────────────────────────────

/** Lora - display serif for screen titles, slide headers, and hero text */
export const SERIF = 'Lora_700Bold';

/** Lora SemiBold - for sub-headers and emphasis */
export const SERIF_SEMI = 'Lora_600SemiBold';

/** Spectral - body serif for all running text and descriptions */
export const BODY = 'Spectral_400Regular';
